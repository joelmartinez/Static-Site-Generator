
<p>
  <a href="http://github.com/joelmartinez" rel="me">GitHub</a>
  <a href="http://twitter.com/joelmartinez" rel="me">&#64;joelmartinez</a>
</p>
  
<a name="archives" />
<h1>Archives</h1>
<div>
  @foreach(var year in Model.Years) {
  <a href="#@year.Name">@year.Name</a> @:|
  }
</div>

@foreach(var year in Model.Years) {
  <a name="@year.Name" />
  <h2>@year.Name</h2>

  <ul id="archivelist">
  @foreach(var month in year.Months) {

  <!--<ul>-->
    foreach(var post in month.Posts) {
    <li>
      <a href="@post.UrlPath">@post.Title</a> (@month.Name)
    </li>
    }
 <!-- </ul>-->

  }
  </ul>
}